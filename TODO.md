# TODO - Fix Work Order Name Auto-fill

## Task
In Workorder Details, the Work Order Name should not be auto-filled. The user should type it manually.

## Steps
- [x] 1. Read and understand the add-workorder component files
- [x] 2. Identify the auto-fill logic in `getClient()`
- [x] 3. Remove auto-fill logic that sets the `name` control value
- [x] 4. Remove unused `workOrderPrefix` variable and `generateClientPrefix()` method
- [x] 5. Verify the changes
